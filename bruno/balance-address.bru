meta {
  name: POST /wallet/address
  type: http
}

post {
  url: {{baseUrl}}/wallet/address
  body: json
}

body:json {
  {
    "unlinkMnemonic": "{{unlinkMnemonic}}",
    "evmPrivateKey": "{{evmPrivateKey}}"
  }
}
